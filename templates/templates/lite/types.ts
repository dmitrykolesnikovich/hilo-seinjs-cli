/**
 * @File   : types.ts
 * @Author : {{AUTHOR.NAME}} ({{AUTHOR.EMAIL}})
 * @Date   : {{DATE}}
 * @Description: types.
 */
export enum EModelEvents {
  New = 'New',
  LoadStart = 'LoadStart',
  Loading = 'Loading',
  LoadEnd = 'LoadEnd',
  Error = 'Error'
}
